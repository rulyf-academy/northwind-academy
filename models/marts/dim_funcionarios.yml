version: 2

models:
  - name: dim_funcionarios
    columns:
      - name: pk_funcionario
        description: Chave primária da tabela dim_funcionarios
        tests:
          - unique 
          - not_null
